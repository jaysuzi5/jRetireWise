apiVersion: v1
kind: Secret
metadata:
  name: jretirewise-secret
  namespace: default
type: Opaque
stringData:
  # Django secret key - should be generated with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
  SECRET_KEY: "change-me-to-random-secret-key-in-production"

  # Database credentials
  DATABASE_PASSWORD: "change-me-to-secure-password"

  # Email credentials
  EMAIL_HOST_USER: "your-email@gmail.com"
  EMAIL_HOST_PASSWORD: "your-app-specific-password"

  # Google OAuth (if using social auth)
  SOCIAL_AUTH_GOOGLE_OAUTH2_KEY: "your-google-oauth2-key"
  SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET: "your-google-oauth2-secret"

  # Database URL (optional, can be constructed from parts above)
  # Format: postgresql://user:password@host:port/dbname
  DATABASE_URL: "postgresql://jretirewise:change-me-to-secure-password@postgres-service:5432/jretirewise"

  # AWS S3 credentials (if using S3 for static/media files)
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
  AWS_STORAGE_BUCKET_NAME: ""

  # Sentry DSN (for error tracking)
  SENTRY_DSN: ""
