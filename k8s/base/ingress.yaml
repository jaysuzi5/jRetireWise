apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jretirewise
  labels:
    app: jretirewise
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
  - host: jaycurtis.org
    http:
      paths:
      - path: /jretirewise/
        pathType: Prefix
        backend:
          service:
            name: prod-jretirewise-jretirewise-web-service
            port:
              name: http
